# API Gateway Structure

See `api-gateway-overview.md` for a summary of endpoint structure and auth.
Main resources: - /cart/quote (POST, OPTIONS) - /dashboard/update-profile (POST, OPTIONS) - /auth/login (POST, OPTIONS) - /auth/signup (POST, OPTIONS) - /dashboard/profile (GET, OPTIONS) - /dashboard/orders (GET, OPTIONS)
