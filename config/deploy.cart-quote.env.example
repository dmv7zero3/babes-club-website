# Copy to config/deploy.cart-quote.env and customise for the target environment.
AWS_REGION=us-east-1
LAMBDA_NAME=babes-cart-quote
LAYER_NAME=marketbrewer-commerce-core
REST_API_ID=a2fps4r1la
STAGE_NAME=prod
SITE_DOMAIN=www.thebabesclub.com
ALARM_PREFIX=cart-quote
# SNS topic ARN that receives CloudWatch alarm notifications (optional).
# ALARM_TOPIC_ARN=arn:aws:sns:us-east-1:123456789012:alerts
# Extra Lambda layer ARNs to include alongside the commerce core layer (comma-separated).
# EXTRA_LAYER_ARNS=arn:aws:lambda:us-east-1:123456789012:layer:other:1
# Set SKIP_LAYER=true to reuse the latest published layer version.
# SKIP_LAYER=true
# Set to populate Lambda environment variables when invoking AWS CLI directly.
# CORS_ALLOW_ORIGIN=https://thebabesclub.com,https://www.thebabesclub.com
# QUOTE_SIGNATURE_SECRET_PARAMETER=/commerce/cart-quote/signing-secret
